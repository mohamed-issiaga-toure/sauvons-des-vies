/**
 * Volunteer — page "Nous rejoindre" (bénévolat).
 * Sections utilisées : HeroSection, ContactForm (subject="volunteer").
 */

export default function Volunteer() {
  // TODO : à implémenter ensemble
  return null;
}
